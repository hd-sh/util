# util

# Example Usage

```javascript
const Util, { isIOS,.... } = require('@hd-sh/util')

Util.getStyle

isIOS

```

## or

```javascript
const { isIOS, getStyle,.... } = require('@hd-sh/util')
isIOS
getStyle('dom','xx')

```

```javascript

  isIOS,
  isAndroid,
  isIE,
  delay,
  sleep,
  generateRange,
  importScript,
  makeCancelablePromise,
  formatRequestData,
  formatPhoneDisplay,
  phoneDesensitize,
  throttle,
  stabilization,
  debounce,
  copyText,
  generateUuid,
  isEmptyArray,
  isEmptyObject,
  isEmptyValue,
  isGeneratorFunction,
  isNotNumber,
  noopValidator,
  patterns,
  validators,
  typeFn,
  sum,
  getStyle,
```
